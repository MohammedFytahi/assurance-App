<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_5_0.xsd"
         version="5.0">

    <servlet>
        <servlet-name>springDispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring-servlet.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>springDispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>


    <jsp-config>
        <taglib>
            <taglib-uri>https://jakarta.ee/jsp/jstl/core</taglib-uri>
            <taglib-location>/WEB-INF/lib/c.tld</taglib-location>
        </taglib>
    </jsp-config>

    <!-- Remove Spring Security filter configuration -->
    <!-- The springSecurityFilterChain has been removed -->

    <!-- Remove the custom AuthenticationFilter if it is not needed -->
    <!-- The authFilter has been removed -->
    <filter>
        <filter-name>authFilter</filter-name>
        <filter-class>com.example.demomvc.filter.AuthenticationFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>authFilter</filter-name>
        <url-pattern>/*</url-pattern> <!-- Apply to all requests -->
    </filter-mapping>

</web-app>
